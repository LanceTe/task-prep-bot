Drop PNG/GIF/JPEG/WEBP files here. Each file's name (without extension)
becomes the Discord emoji name. Run `uv run python scripts/seed_emojis.py`
to upload any that aren't already on the application.
